import { Text, View } from "react-native";

export default function Index() {
  return (
    <View className="flex-1 items-center justify-center bg-white-300">
      {/* TODO: Add a welcome logo animation and transition to the login screen */}
    </View>
  );
}
